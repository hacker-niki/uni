# Удаление поля сложности из вопросов

## Выполненные изменения

### ✅ 1. База данных

**Удалено поле `difficulty` из таблицы `questions`:**

```sql
ALTER TABLE questions DROP COLUMN difficulty;
```

**Новая структура таблицы:**
- `id` - уникальный идентификатор
- `question_text` - текст вопроса
- `source_document_id` - связь с исходным документом
- `creator_id` - создатель вопроса
- `is_approved` - одобрен ли вопрос
- `approved_by` - кто одобрил
- `approved_at` - когда одобрен
- `created_at` - дата создания
- `updated_at` - дата обновления

### ✅ 2. Файл инициализации БД

**Обновлен `database/init.sql`:**

- Удалено поле `difficulty` из CREATE TABLE
- Удалено поле из всех INSERT statements
- Удален индекс `idx_difficulty`
- Удален enum тип `DifficultyLevel`

### ✅ 3. Backend модели

**Обновлен `backend/models.py`:**

```python
class Question(Base):
    __tablename__ = "questions"

    id = Column(BigInteger, primary_key=True, autoincrement=True)
    question_text = Column(Text, nullable=False)
    source_document_id = Column(BigInteger, ForeignKey("source_documents.id"))
    creator_id = Column(BigInteger, ForeignKey("users.id"), nullable=False)
    is_approved = Column(Boolean, nullable=False, default=False)
    approved_by = Column(BigInteger, ForeignKey("users.id"))
    approved_at = Column(TIMESTAMP, nullable=True)
    created_at = Column(TIMESTAMP, nullable=False, server_default=text("CURRENT_TIMESTAMP"))
    updated_at = Column(TIMESTAMP, nullable=False,
                       server_default=text("CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP"))
```

**Удалено:**
- Поле `difficulty`
- Enum класс `Difficulty`
- Index на `difficulty`

### ✅ 4. API endpoints

**Обновлен `backend/main.py`:**

**QuestionResponse модель:**
```python
class QuestionResponse(BaseModel):
    id: int
    question: str
    answers: List[AnswerOptionResponse]
    is_approved: bool
    # difficulty поле удалено
```

**Endpoint `/api/questions`:**
- Удален параметр фильтрации `difficulty`
- Удалено поле `difficulty` из ответа

## Тестирование

### Проверка структуры таблицы:

```bash
docker exec testgen-db mariadb -utestgen_user -ptestgen_pass testgen -e "DESCRIBE questions;"
```

**Результат:** ✅ Поле `difficulty` отсутствует

### Проверка API:

```bash
curl http://localhost:8000/api/questions
```

**Результат:** ✅ API возвращает вопросы без поля `difficulty`

### Health check:

```bash
curl http://localhost:8000/health
```

**Результат:**
```json
{
  "status": "healthy",
  "database": "connected",
  "statistics": {
    "users": 4,
    "questions": 10,
    "tests": 1
  }
}
```

## Результаты

✅ Поле сложности успешно удалено из:
- Базы данных (таблица questions)
- Скрипта инициализации (init.sql)
- ORM моделей (models.py)
- API моделей и endpoints (main.py)

✅ Все тестовые данные сохранены (10 вопросов)

✅ API работает корректно

✅ Система готова к дальнейшей работе без поля сложности вопросов
